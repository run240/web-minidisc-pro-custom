name: Build Windows (optional SignPath signing)

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
    tags:
      - 'v*'

permissions:
  actions: read
  contents: read

jobs:
  build-and-sign:
    runs-on: windows-latest

    steps:
      - name: Check out source and submodules
        uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            package-lock.json
            webminidisc/package-lock.json

      - name: Install application dependencies
        run: npm ci --legacy-peer-deps

      - name: Run Hi-MD edit regression test
        run: npm run test:himd-edit-batch

      - name: Build unpacked Windows application
        run: npm run pack:custom

      - name: Verify and package Windows release
        shell: pwsh
        run: ./scripts/package-windows-release.ps1

      - name: Stage unsigned application
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path staging | Out-Null
          Copy-Item -Recurse -Force build\win-unpacked\* staging\

      - name: Upload unsigned artifact
        id: upload-unsigned
        uses: actions/upload-artifact@v7
        with:
          name: web-minidisc-pro-windows-unsigned
          path: staging

      - name: Upload release ZIP and checksum
        uses: actions/upload-artifact@v7
        with:
          name: web-minidisc-pro-windows-release
          path: build/release/*

      - name: Submit signing request
        if: ${{ github.event_name != 'pull_request' && vars.SIGNPATH_ORGANIZATION_ID != '' }}
        uses: signpath/github-action-submit-signing-request@v2
        with:
          api-token: '${{ secrets.SIGNPATH_API_TOKEN }}'
          organization-id: '${{ vars.SIGNPATH_ORGANIZATION_ID }}'
          project-slug: '${{ vars.SIGNPATH_PROJECT_SLUG }}'
          signing-policy-slug: '${{ vars.SIGNPATH_SIGNING_POLICY_SLUG }}'
          artifact-configuration-slug: '${{ vars.SIGNPATH_ARTIFACT_CONFIGURATION_SLUG }}'
          github-artifact-id: '${{ steps.upload-unsigned.outputs.artifact-id }}'
          wait-for-completion: true
          output-artifact-directory: '${{ github.workspace }}\signed'

      - name: Verify signed executables
        if: ${{ github.event_name != 'pull_request' && vars.SIGNPATH_ORGANIZATION_ID != '' }}
        shell: pwsh
        run: |
          $targets = @(
            'signed\Web MiniDisc Pro.exe'
          )
          foreach ($target in $targets) {
            $signature = Get-AuthenticodeSignature -LiteralPath $target
            if ($signature.Status -ne 'Valid') {
              throw "Invalid signature: $target ($($signature.Status))"
            }
          }

      - name: Upload signed application
        if: ${{ github.event_name != 'pull_request' && vars.SIGNPATH_ORGANIZATION_ID != '' }}
        uses: actions/upload-artifact@v7
        with:
          name: web-minidisc-pro-windows-signed
          path: signed
